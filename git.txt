https://docs.npmjs.com/resolving-eacces-permissions-errors-when-installing-packages-globally
npm run build
cd ~/workspace
git remote add origin git@github.com:deffyc/tushu.git
git add *
git commit -m 'upload'
git push -u origin master:src -f

cd dist
git add *
git commit -m 'upload'
git push -u origin master -f